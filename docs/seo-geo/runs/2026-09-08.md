# LoanInCA SEO/GEO 执行记录 — 2026-09-08

## 授权与范围

- 按 `D:\download\SEO_GEO_CODEX_SOP.md` 执行。
- 目标站点已确认是 `loaninca.com`；本轮只在 `codex/reader-content-20260906` 本地工作区修改。
- 未发布、未 `git push`、未创建 PR、未改 DNS/robots 策略、未提交索引、未提交真实表单。

## 本轮完成

1. **更新时间信号同步（P1）**
   - 更新 `orange-county-mortgage.html` 与 3 篇已在人性化提交中修改的文章：页面可见更新日期、JSON-LD `dateModified`（Orange County 另含 `article:modified_time`）统一为 `2026-09-08`。
   - 更新 `sitemap.xml` 对应 4 个 URL 的 `lastmod` 为 `2026-09-08`；保留原 `datePublished`，没有批量刷新未修改页面。
2. **GEO 规范入口清理（P1）**
   - 重写 `llms.txt`、`llms-full.txt`，移除旧服务/旧文章重定向 URL，改为 sitemap 中可核验的当前规范入口；保持品牌、NMLS、服务范围和联系方式事实，不添加资质或收益承诺。
   - 在 `scripts/validate-site.mjs` 增加同域 URL 与 sitemap 规范 URL 的回归检查。
3. **新闻分享元数据（P1）**
   - 修正 `news.html` 的 `og:title` 与 `og:description`，与资讯页的 `<title>`、description 和 JSON-LD 主题一致。
   - 同一验证脚本增加新闻页 OG 与页面标题/描述一致性检查。

## 验证

- `node scripts/validate-site.mjs`：通过。
- `node --test tests/*.test.mjs`：15/15 通过。
- 隔离回归：临时把 `llms.txt` 换成旧 `/refinance-california`，验证脚本按预期失败；恢复后通过。
- 隔离回归：临时把新闻 `og:title` 改成错误值，验证脚本按预期失败；恢复后通过。
- 日期核对：4 个页面的 JSON-LD/可见日期（及适用的 OG 日期）与 sitemap 均为 `2026-09-08`。
- `git diff --check`：无空白错误。
- 生产只读审计 `node scripts/audit-production.mjs https://www.loaninca.com`：35 个 sitemap URL、0 个页面状态异常、0 个风险表述命中；唯一 404 是 Cloudflare Email Protection 注入的 `/cdn-cgi/l/email-protection`，不是仓库业务链接。审计因此返回非零，但该结果已记录并未改动 Cloudflare 内容。
- 未测：GSC/GA4/Bing/AI 引用数据、手机真机截图、生产新版本（因为本轮未发布）。

## 回滚

- 按文件回退本轮 9 个修改文件及 `docs/seo-geo/` 记录即可；不触及用户已有提交或生产环境。
- 如果只需回退单项：日期项回退 4 个页面和 sitemap；GEO 项回退两份 llms 与验证脚本；新闻项回退 `news.html` 与验证脚本对应检查。

## 下一轮续接（最多 3 项）

1. 在取得 GSC/GA4 授权后，按 SOP 拉取两个完整 28 天区间的站点、页面和查询数据，补齐真实优先级。
2. 对 `/mortgage/dscr/`、`/mortgage/self-employed/`、`/mortgage/h1b-opt/`、`/mortgage/jumbo/`、`/mortgage/refinance/` 做移动端/键盘/CTA 实测，并只修复有证据的问题。
3. 单独只读核查 `/news` 的每周自动更新链路（来源、日期、失败回退和重复项），必要时再提交小范围修复方案；未获确认前不改生产抓取频率。

## 阻塞与待确认

- 需要用户确认后才能发布或推送本地修改。
- GSC/GA4/Bing/AI 引用平台尚未连接。
- `robots.txt` AI 爬虫访问策略本轮未改，需业务方单独确认后评估。

## 用户确认后的发布（2026-09-08）

- 用户明确回复“递交吧”后，已将提交 `a6cf3c5` 推送到 `origin/main`，触发原有 Cloudflare Git 发布链；没有 force push 或改动其他站点。
- 生产复核：`https://www.loaninca.com/news` 的 OG 标题/摘要已与新闻页面一致；`/blog/appraisal-below-purchase-price-california` 与 `/orange-county-mortgage` 的可见更新日期和 `dateModified` 已为 `2026-09-08`。
- GitHub `main` 原始文件已包含规范化后的 `llms.txt` 与四个 sitemap `lastmod` 更新。发布后仍需等待搜索引擎重新抓取，不能把上线等同于收录或排名变化。

## 继续核查与本地修复（2026-09-08）

### 新闻自动更新频率（P1）

- 只读核查发现 `.github/workflows/update-mortgage-news.yml` 原有 3 个定时表达式，和产品要求“每周 1 次”不一致；同一工作流仍保留 `push`（代码变更）与 `workflow_dispatch`（人工补跑）入口。
- 本地将定时触发收敛为每周四 17:30 UTC 运行 1 次，时间点位于 Freddie Mac PMMS 周度发布之后；未改变来源、抓取逻辑、页面结构或手动补跑能力。
- 在 `tests/news.test.mjs` 增加回归断言，防止以后再次出现多于 1 个 schedule。

### 验证

- `node scripts/validate-site.mjs`：通过。
- `node --test tests/*.test.mjs`：16/16 通过（含新增“单一周调度”断言）。
- `NEWS_VERIFY_ATTEMPTS=2 node scripts/verify-news-live.mjs`：通过，生产新闻页与 5 条来源记录一致。
- `git diff --check`：通过。
- 本轮修改尚未推送/发布；生产只读检查未向表单、索引或外部数据写入。

### 下一步（待用户确认发布后）

1. 将本地工作流频率修复提交并推送到 `main`，再观察一次 GitHub Actions 手动运行结果。
2. 保持每周一次的计划触发；仅在 PMMS 来源变更或手动补跑时额外运行，不把“无数据变化”伪造成新文章。
3. 取得 GSC/GA4 授权后，补真实搜索与转化基线；目前不编造流量、排名或 AI 引用数据。
